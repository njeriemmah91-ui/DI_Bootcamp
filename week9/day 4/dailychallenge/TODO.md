# Week 9 Day 4 - Daily Challenge: Age Tracker (Redux Toolkit + Thunk)

## Steps
- [x] Create Vite + React project structure in `week9/day 4/dailychallenge`

- [x] Add Redux Toolkit store (`configureStore`)

- [x] Implement `ageUpAsync` and `ageDownAsync` with `createAsyncThunk` + delay

- [x] Implement `age` slice with loading state + `extraReducers`

- [x] Create `AgeDisplay` component with spinner while loading

- [x] Create `AgeControls` component with Age Up / Age Down buttons

- [x] Wire Redux with `Provider`

- [x] Add basic styling

- [ ] (Optional) Run `npm install` and `npm run dev` to verify

